/**
 * Build-time application version, injected by Vite from package.json.
 * In tests it resolves to the placeholder defined in vitest.config.ts.
 */
export const appVersion: string = __APP_VERSION__
